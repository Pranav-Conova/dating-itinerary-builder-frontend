services:
  - type: web
    name: dating-itinerary-builder-frontend
    env: static
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://your-backend.onrender.com
